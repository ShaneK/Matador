#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-dustjs/bin/grunt-dustjs" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-dustjs/bin/grunt-dustjs" "$@"
  ret=$?
fi
exit $ret
